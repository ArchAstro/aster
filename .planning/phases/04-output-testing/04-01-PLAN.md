---
phase: 04-output-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/cli/commands.rs
  - src/cli/mod.rs
  - src/cli/output.rs
  - src/main.rs
autonomous: true

must_haves:
  truths:
    - "All commands support --json flag for machine-readable output"
    - "--verbose streams all output with project prefixes"
    - "--quiet shows only final pass/fail summary"
    - "--help shows clear usage for all commands"
  artifacts:
    - path: "src/cli/output.rs"
      provides: "OutputMode enum and JSON output helpers"
      exports: ["OutputMode", "output_json"]
    - path: "src/cli/commands.rs"
      provides: "Global --json, --quiet flags"
      contains: "#[arg(long, global = true)]"
  key_links:
    - from: "src/cli/commands.rs"
      to: "src/cli/output.rs"
      via: "OutputMode derivation from flags"
      pattern: "output_mode.*OutputMode"
    - from: "src/main.rs"
      to: "src/cli/output.rs"
      via: "JSON output for list/graph/why commands"
      pattern: "output_json"
---

<objective>
Add global CLI flags (--json, --quiet) and implement JSON output for all commands

Purpose: Enable machine-readable output for CI/CD integration and scripting, plus verbosity control for different use cases (verbose for debugging, quiet for CI)
Output: All commands support `--json` flag outputting structured JSON; `--quiet` shows minimal output; `--verbose` behavior improved with project prefixes
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-output-testing/04-CONTEXT.md
@.planning/phases/04-output-testing/04-RESEARCH.md

# Existing CLI structure
@src/cli/commands.rs
@src/cli/mod.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global flags and OutputMode</name>
  <files>
    - Cargo.toml
    - src/cli/commands.rs
    - src/cli/output.rs
    - src/cli/mod.rs
  </files>
  <action>
1. Add `console = "0.16"` to Cargo.toml dependencies (will be used for colors in later tasks, but add now for OutputMode terminal detection)

2. Create `src/cli/output.rs` with:
   - `OutputMode` enum: `Normal`, `Verbose`, `Quiet`, `Json`
   - `output_json<T: Serialize>(value: &T) -> anyhow::Result<()>` helper that:
     - Uses `serde_json::to_writer_pretty` if stdout is terminal
     - Uses `serde_json::to_writer` (compact) if piped
     - Ensures trailing newline
   - JSON output structs for all commands:
     - `ProjectInfo` with address, path, plugin, targets (for `list --json`)
     - `GraphOutput` with nodes (Vec<String>), edges (HashMap<String, Vec<String>>) for adjacency list
     - `WhyOutput` with from, to, path (Option<Vec<String>>)
     - `ExecutionOutput` with results (nested by project), summary
     - `TargetResult` with status, exit_code, duration_ms
     - `ExecutionSummary` with total, passed, failed, skipped, duration_ms

3. Update `src/cli/commands.rs`:
   - Add `--quiet` flag: `#[arg(short, long, global = true, conflicts_with = "verbose")]`
   - Add `--json` flag: `#[arg(long, global = true)]`
   - Add `output_mode(&self) -> OutputMode` method to Cli struct
   - OutputMode::Json if json flag, Verbose if verbose, Quiet if quiet, else Normal

4. Update `src/cli/mod.rs` to export output module and OutputMode
  </action>
  <verify>
    - `cargo build` compiles without errors
    - `cargo test` passes existing tests
    - `aster --help` shows --json, --quiet, --verbose flags
  </verify>
  <done>
    - OutputMode enum exists with 4 variants
    - Global flags added to CLI
    - JSON output helpers ready for use
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement JSON output for introspection commands</name>
  <files>
    - src/main.rs
  </files>
  <action>
1. Update `Commands::List` handler:
   - If `output_mode == Json`:
     - Build Vec<ProjectInfo> from discovered projects
     - Call `output_json(&projects)` and return
   - Else: existing text output

2. Update `Commands::Graph` handler:
   - If `output_mode == Json`:
     - Build GraphOutput with nodes (all target addresses) and edges (adjacency list)
     - For specific target: only include that target's subgraph
     - Call `output_json(&graph_output)` and return
   - Else: existing text output

3. Update `Commands::Why` handler:
   - If `output_mode == Json`:
     - Build WhyOutput with from, to, path (None if no path found)
     - Call `output_json(&why_output)` and return
   - Else: existing text output

4. Update verbose mode output:
   - In verbose mode, prefix diagnostic messages consistently: "[aster]" prefix
   - Use eprintln! for all diagnostic output (not stdout) to keep JSON clean

Important: JSON output goes ONLY to stdout. All other output (progress, diagnostics, errors) goes to stderr. This allows `aster list --json | jq .` to work correctly.
  </action>
  <verify>
    - `aster list --json` outputs valid JSON array of projects
    - `aster graph --json` outputs valid JSON with nodes and edges
    - `aster why //a //b --json` outputs valid JSON with path
    - `aster list --json | jq .` works (no non-JSON output on stdout)
    - `aster list --verbose` shows diagnostic messages on stderr
  </verify>
  <done>
    - list, graph, why commands all support --json
    - JSON output is valid and parseable
    - Verbose mode uses stderr for diagnostics
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement JSON and quiet output for execution commands</name>
  <files>
    - src/main.rs
    - src/cli/output.rs
  </files>
  <action>
1. Update `Commands::Run` handler:
   - Pass output_mode to execution flow
   - If `Json`: collect all results, then output_json(ExecutionOutput) at end
   - If `Quiet`: suppress per-project output, only show final summary line
   - If `Verbose`: keep current behavior (per-project output as it happens)
   - If `Normal`: keep current behavior for now (enhanced in next plan)

2. Update `Commands::Affected` handler:
   - Same pattern as Run: respect output_mode
   - If `Json`: output_json(ExecutionOutput) with affected info
   - If `Quiet`: minimal output
   - If `Verbose`: detailed per-project output

3. Add helper in output.rs:
   - `fn print_summary(results: &[ExecutionResult], mode: OutputMode)` that:
     - Quiet: "X passed, Y failed" single line
     - Normal/Verbose: current detailed summary
     - Json: no text output (JSON already printed)

4. Ensure exit code 1 if any target fails (already implemented, verify still works with all modes)
  </action>
  <verify>
    - `aster test --all --json` outputs valid JSON with results per project
    - `aster test --all --quiet` shows only final pass/fail line
    - `aster affected test --base=HEAD --json` outputs valid JSON
    - Exit code is 1 when any target fails, regardless of output mode
    - `cargo test` passes all existing tests
  </verify>
  <done>
    - Run and Affected commands support --json, --quiet modes
    - JSON output includes all execution results
    - Quiet mode shows minimal summary only
    - Exit codes correct in all modes
  </done>
</task>

</tasks>

<verification>
Run these commands to verify Phase 4 Plan 1 is complete:

```bash
# Build passes
cargo build

# All tests pass
cargo test

# --help shows global flags
aster --help | grep -E "(json|quiet|verbose)"

# JSON output works for introspection commands
aster list --json | jq 'type'  # Should output "array"
aster graph --json | jq 'has("nodes")' # Should output "true"

# Quiet mode shows minimal output
aster list --quiet  # Should show nothing or minimal summary

# Verbose mode uses stderr
aster list --verbose 2>&1 | grep "Workspace root"
```
</verification>

<success_criteria>
- [ ] --json flag available on all commands (global)
- [ ] --quiet flag available on all commands (global)
- [ ] --verbose and --quiet are mutually exclusive
- [ ] `aster list --json` outputs JSON array of projects
- [ ] `aster graph --json` outputs JSON adjacency list
- [ ] `aster why --json` outputs JSON path
- [ ] `aster test --json` outputs JSON execution results
- [ ] `aster affected --json` outputs JSON execution results
- [ ] Quiet mode suppresses per-project output
- [ ] All diagnostic output uses stderr, not stdout
- [ ] Exit code 1 on failures works in all modes
</success_criteria>

<output>
After completion, create `.planning/phases/04-output-testing/04-01-SUMMARY.md`
</output>
